# 🎨 AI Resume Builder - Frontend Setup Instructions

## 📦 Что включено:

Frontend приложение на React с TypeScript полностью настроено и готово к использованию!

### ✅ Реализовано:
- **Аутентификация** (Login/Register)
- **Dashboard** с статистикой
- **Защищенные роуты**
- **Sidebar навигация**
- **UI компоненты** (Button, Input, Card)
- **API клиент** с interceptors
- **State management** (Zustand)
- **TailwindCSS** для стилей
- **Responsive дизайн**

## 🚀 Как запустить:

### 1. Распакуйте архив в папку вашего проекта:
```bash
# В папке ai-resume-builder (где находится backend)
tar -xzf ai-resume-frontend.tar.gz
cd frontend
```

### 2. Установите зависимости:
```bash
npm install
```

### 3. Настройте переменные окружения:
```bash
# Создайте .env файл (он уже создан с правильными настройками)
# Убедитесь что backend запущен на порту 8080
```

### 4. Запустите backend (в отдельном терминале):
```bash
cd ../backend
docker-compose up -d  # База данных
go run cmd/main.go     # API сервер
```

### 5. Запустите frontend:
```bash
cd ../frontend
npm run dev
```

### 6. Откройте в браузере:
```
http://localhost:5173
```

## 🎯 Тестирование:

1. **Регистрация нового пользователя:**
   - Перейдите на `/auth/register`
   - Заполните форму
   - После успешной регистрации вы будете перенаправлены на Dashboard

2. **Вход в систему:**
   - Используйте email и пароль
   - JWT токен сохраняется автоматически

3. **Dashboard:**
   - Показывает статистику пользователя
   - Quick Actions для быстрого доступа
   - Информация о кредитах (2 бесплатные генерации)

## 📁 Структура проекта:

```
ai-resume-builder/
├── backend/          # ✅ Backend (уже готов)
│   ├── cmd/
│   ├── internal/
│   └── ...
├── frontend/         # 🎨 Frontend (этот архив)
│   ├── src/
│   │   ├── api/           # API клиент
│   │   ├── app/           # Роутинг
│   │   ├── features/      # Модули (auth, profile, etc.)
│   │   ├── pages/         # Страницы
│   │   ├── shared/        # Общие компоненты
│   │   └── styles/        # Стили
│   └── package.json
└── README.md
```

## 🔧 Следующие шаги разработки:

### Phase 2: Profile Management
- [ ] Форма редактирования профиля
- [ ] CRUD для опыта работы
- [ ] CRUD для образования
- [ ] Управление навыками

### Phase 3: AI Generation
- [ ] Форма генерации резюме
- [ ] Форма генерации cover letter
- [ ] Выбор шаблонов
- [ ] Preview генерированных документов

### Phase 4: History & Export
- [ ] Страница истории документов
- [ ] Экспорт в PDF
- [ ] Поиск и фильтры

## 🐛 Возможные проблемы:

### CORS ошибки:
Убедитесь что backend настроен правильно в `backend/cmd/main.go`:
```go
c := cors.New(cors.Options{
    AllowedOrigins: []string{"http://localhost:5173"},
    AllowedMethods: []string{"GET", "POST", "PUT", "DELETE", "OPTIONS"},
    AllowedHeaders: []string{"*"},
    AllowCredentials: true,
})
```

### Ошибка подключения к API:
- Проверьте что backend запущен: `http://localhost:8080/health`
- Проверьте .env файл: `VITE_API_BASE_URL=http://localhost:8080/api/v1`

### База данных:
- Убедитесь что Docker запущен
- Проверьте миграции выполнены
- PostgreSQL работает на порту 5433

## 📞 Поддержка:

Если возникнут вопросы:
1. Проверьте логи backend: `docker logs resumebuilder-postgres`
2. Проверьте консоль браузера на ошибки
3. Убедитесь что все зависимости установлены: `npm install`

## 🎉 Готово!

Теперь у вас есть полностью работающий frontend для AI Resume Builder!

### Команды для разработки:
```bash
npm run dev        # Запуск dev сервера
npm run build      # Production build
npm run preview    # Preview production build
npm run lint       # Проверка кода
npm run format     # Форматирование
```

Удачной разработки! 🚀
